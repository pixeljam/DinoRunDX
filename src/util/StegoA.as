package util {		import flash.geom.Point;		import supers.MovingClip;		public class StegoA extends MovingClip {				private var dad:Object;				public function StegoA (o,br):void {						//init			super.init (o,br,this);						//extra physics properties			ph.sh = "circ";		//shape			ph.r = o.r;			//radius			ph.m = 200;			ph.f = .9,			//friction			ph.bn = 0,			//bounce						//misc			ride=true;			dad=o.dad;			big=true;			o.typ="Dino";						}				override public function nulls () {						super.nulls();			dad=null;					}				public function main ():void {						//center is always 39 down, perp to stego center			pos = dad.pos.add (Point.polar(40, dad.ang+(Math.PI/2)));						//vel			vel.x=dad.vel.x;			vel.y=dad.vel.y;						dad.RR=RR;					}				public function bounce (v) {						dad.bounce (v);					}				public function crush (obj) {						dad.crush(obj);					}				override public function applyVel (v:Point) {						dad.applyVel (v);					}			}	}
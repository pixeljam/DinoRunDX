package fx {		import flash.display.MovieClip;		public class Skid {				private var clip:MovieClip;		private var c:int=0;		private var accelDec:Number;		private var r:Number;						public function Skid (clip:MovieClip):void {						this.clip=clip;			r=clip.ph.r-6;					}				public function main () {									if (clip.near) { if (rand(0,1)<.8) { clip.brain.create ( {nam:"Grass", ug:clip.ug, px:clip.pos.x+rand(-3,3), py:clip.pos.y+r-3, vx:rand(clip.vel.x*.9,clip.vel.x*1.5), vy:rand(-2.5,1), depth:["world","main"]} ) } }						//remove?			c++;			if (c>=30) { remove() }					}				// give an unrounded random number		private function rand(min:Number, max:Number):Number {			return (Math.random()*(max-min))+min;		}				public function remove () {						clip.removeSYS ("skid"); 			clip=null;					}			}	}